[project]
name = "gallows"
version = "0.1.0"
description = "Стандартная реализация игры Виселица в консольном режиме"
readme = "README.md"
requires-python = ">=3.14"
dependencies = []

[dependency-groups]
dev = [
    "pytest>=9.0.2",
]
